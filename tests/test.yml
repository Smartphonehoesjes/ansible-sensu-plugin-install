---
- hosts: localhost

  vars:
    sensu_client_plugins:
      - name: cpu-checks
        version: 1.0.0
      - name: disk-checks
        version: 2.0.1
      - name: http
        version: 1.0.0
      - name: memory-checks
        version: 1.0.2
      - name: ntp
        version: 1.0.0

  roles:
    - role_under_test
    - 030.sensu-install
